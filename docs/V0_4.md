# v0.4 — Fechamento do Pilar 1

Agora a síntese do Posicionamento Único é persistida no banco e as próximas ações viram tarefas.

## Novas rotas
- `GET /api/v1/strategy/positioning`
- `GET /api/v1/strategy/action-plan`
- `PATCH /api/v1/strategy/action-plan/{item_id}`

Status aceitos no PATCH:
```json
{"status":"pending"}
```
```json
{"status":"in_progress"}
```
```json
{"status":"done"}
```

## Teste
1. Conclua as perguntas do Pilar 1.
2. Execute `POST /api/v1/diagnosis/positioning/synthesize`.
3. Consulte `GET /api/v1/strategy/positioning`.
4. Consulte `GET /api/v1/strategy/action-plan`.
5. Pegue um `id` de tarefa e altere o status com PATCH.

A estrutura inicial do Pilar 2 — Promessa Atrativa — já foi preparada em `app/ai/pillar2.py` e será conectada às rotas na próxima versão.
