# v0.3 — IA

No `.env`:

```env
OPENAI_API_KEY=sua_chave_aqui
OPENAI_MODEL=gpt-5.6-luna
AI_ENABLED=true
```

Depois: `docker compose down`, `docker compose build --no-cache`, `docker compose up`.

Teste `GET /api/v1/ai/status`. A resposta deve trazer `configured: true`.

Novas rotas: `GET /api/v1/diagnosis/positioning/ai-history` e `POST /api/v1/diagnosis/positioning/synthesize`.
